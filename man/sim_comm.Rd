% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulations.R
\name{sim_comm}
\alias{sim_comm}
\title{Simulate community with fluctuations in species abundances across time}
\usage{
sim_comm(
  years = 25,
  n_sp = 10,
  even = 0.6,
  tot_abu = 300,
  power = 1.8,
  mean_env_resp = 0,
  sd_env_resp = 0.01,
  bimodal_env = FALSE,
  comp = FALSE,
  mean_trend_resp = 0,
  sd_trend_resp = 0.01,
  bimodal_trend = FALSE,
  bound_pos = TRUE
)
}
\arguments{
\item{years}{Numeric. The length of the timeseries in years.}

\item{n_sp}{Numeric. Number of species in the community.}

\item{even}{Numeric. Maximum relative abundance for the species in the community.}

\item{tot_abu}{Numeric. Total abundance of the community, representing e.g. the number of
individuals or the total amount of biomass.}

\item{power}{Numeric. The slope of the relationship between log(mean) and log(variance) of
the abundances of the species.}

\item{mean_env_resp}{Numeric. The mean of the normal distribution from which each of the
species responses to the environemtnal cue is drawn.}

\item{sd_env_resp}{Numeric. The standard deviation around the mean of the normal distribution
from which each of the species responses to the environemtnal cue is drawn.}

\item{bimodal_env}{Boolean. Making the reponse to the environmental cue either
uniform among the species (if FALSE), or making the majority of half of the
species respond positively, and the other half negatively. Default FALSE.}

\item{comp}{Boolean. If TRUE, species exhibit compensatory dynamics, i.e. the gain
of abundance in a species from one year to the next, is compensated by the
loss of abundance in another species, where the latter has a similar mean
abundance value. Default FALSE.}

\item{mean_trend_resp}{Numeric. The mean of the normal distribution from which each of the
species responses to the longterm trend is drawn. Default is 1.}

\item{sd_trend_resp}{Numeric. The standard deviation around the mean of the normal
distribution from which each of the species responses to the longterm trend is
drawn. Default is 1.}

\item{bimodal_trend}{Boolean. If TRUE, the majority of half of the species exhibit a
positive long term trend of abundances, and the other half expresses a
negative long term trend. If FALSE, most of the species exhibit a uniform long
term trend in abundances, depending on the value set for mean_trend and
mean_sd. Default FALSE.}

\item{bound_pos}{Boolean. If TRUE, abundance values that are simulated to be
negative, will be set to zero. Default TRUE.}
}
\value{
A named list with four elements:
\itemize{
\item \code{sim_data}: The simulated temporal community data where species are
columns and years are rows.
\item \code{param_years}: Values for the environmental cue and the trend throughout the
years. Note that these contain values even if the environment or trend are
switched off.
\item \code{param_species}: A data frame containing the responses to the environment and
the long term trend, as well as the mean abundance and its standard deviation
for each species in the community.
\item \code{param_general}: A data frame with only one row, containing all the parameter
settings from the function call.
}
}
\description{
Simulate community with fluctuations in species abundances across time
}
\examples{
require(detrending)

sim_comm(n_sp = 15, years = 30)
}
\author{
Lars Götzenberger, \email{jsegrestin@gmail.com}

Jan Lepš, \email{suspa@prf.jcu.cz}
}
